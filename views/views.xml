<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="jobsite_sila.list">
      <field name="name">jobsite_sila list</field>
      <field name="model">jobsite_sila.jobsite_sila</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="jobsite_sila.action_window">
      <field name="name">jobsite_sila window</field>
      <field name="res_model">jobsite_sila.jobsite_sila</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="jobsite_sila.action_server">
      <field name="name">jobsite_sila server</field>
      <field name="model_id" ref="model_jobsite_sila_jobsite_sila"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="jobsite_sila" id="jobsite_sila.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="jobsite_sila.menu_1" parent="jobsite_sila.menu_root"/>
    <menuitem name="Menu 2" id="jobsite_sila.menu_2" parent="jobsite_sila.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="jobsite_sila.menu_1_list" parent="jobsite_sila.menu_1"
              action="jobsite_sila.action_window"/>
    <menuitem name="Server to list" id="jobsite_sila" parent="jobsite_sila.menu_2"
              action="jobsite_sila.action_server"/>
    -->
  </data>
</odoo>